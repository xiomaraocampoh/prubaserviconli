server:
  port: 8083

spring:
  application:
    name: patient-service

  datasource:
    # Conexión directa al puerto 3307
    url: jdbc:mysql://localhost:3307/serviconli?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: serviconli_pass
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    org.hibernate.SQL: DEBUG
    com.serviconli: DEBUG

jwt:
  secret: 2LM1OEf2filBHvqs2JMdaQBj2sWCqg7gG